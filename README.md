# StudyDev Course Editor

### Run from source

```bash
  pnpm i
  pnpm run start
```

### Publish new version
* Update app/package.json version
* git commit -m "vX.X.X"
* git tag vX.X.X
* git push --follow-tags
